pipeline {
    agent any

     triggers {
        cron('H 8 * * *')  // Tous les jours à 08h00 (heure aléatoire pour éviter les conflits)
    }

    tools {
        maven 'Maven 3.9.9'
        jdk 'JDK 21'
    }

    stages {
        stage('Checkout') {
            steps {
                git 'https://github.com/KhaoulaEsi22/Ing-log.git'
            }
        }

        stage('Build') {
            steps {
                bat 'mvn clean compile'
            }
        }

        stage('Test') {
            steps {
                bat 'mvn test'
            }
        }

        stage('Package') {
            steps {
                bat 'mvn package'
            }
        }

        stage('Deploy') {
            steps {
                bat 'C:\appnutrition\apache-tomcat-11.0.5'
            }
        }
    }

    post {
        success {
            echo ' Build successful!'
        }
        failure {
            echo 'Build failed!'
            mail to: 'messaskhaoula@gmail.com',
                 subject: " Build échoué - OnetoOneShop",
                 body: "Le build Jenkins a échoué. Merci de vérifier le code ou les tests."
        }
    }
}

